import React from 'react';
import LoginSection from '../layouts/ui/LoginSrction/LoginSection';

const Login: React.FC = () => {
  return (
    <section>
      <LoginSection />
    </section>
  );
};

export default Login;
